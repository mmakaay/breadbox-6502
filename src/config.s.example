; BIOS configuration
;
; All named constants used below (HD44780_8BIT, PORTA, P5, etc.)
; are defined in bios/constants.s, which is loaded before this
; file. See that file for the full list of available names.

; --------------------------------------------------------------------
; LCD display
; --------------------------------------------------------------------
;
; The defaults (no config needed) match Ben Eater's tutorial:
; 8-bit data bus on port B, command pins on port A (PA5-PA7).
; See bios/lcd/hd44780_8bit.s for a wiring diagram.
;
; To use a different setup, uncomment one of the presets below
; or write your own.
;
; --------------------------------------------------------------------

; Preset: Ben Eater wiring, 4-bit mode
;
; Same pin layout as the default, but only the upper four data
; lines (PB4-PB7) are used. PB0-PB3 can be disconnected.

;LCD_DRIVER    = HD44780_4BIT
;LCD_CMND_PORT = PORTA
;LCD_DATA_PORT = PORTB
;LCD_PIN_RS    = P5              ; Register Select  (PA5)
;LCD_PIN_RWB   = P6              ; Read/Write       (PA6)
;LCD_PIN_EN    = P7              ; Enable           (PA7)

; Preset: single-port 4-bit mode (all on port B)
;
; Command and data share port B. PB0-PB2 for control, PB4-PB7
; for data, leaving PB3 free for other hardware. Port A is
; entirely available. See bios/lcd/hd44780_4bit.s for a wiring
; diagram.

;LCD_DRIVER    = HD44780_4BIT
